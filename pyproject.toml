[project]
name = "can-log-analyzer"
version = "26.0.2"
description = "CAN log Analyzer"
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "auto-py-utils", email = "chaitu.ycr@gmail.com" }]
keywords = ["python", "CAN", "log", "analyzer"]
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "cantools",
    "python-can",
    "pandas",
    "pyarrow",
    "plotly",
    "nicegui[native]",
    "matplotlib",
]

[dependency-groups]
dev = [
    "pyinstaller",
    "ruff",
    "pytest",
    "pytest-html",
    "pytest-sugar",
    "pytest-clarity",
    "pytest-cov",
    "pytest-watch",
    "mkdocs-puml",
    "mkdocs-exclude",
    "mkdocs-material<10",
    "mkdocs-jupyter",
    "mkdocstrings-python",
    "mkdocs-macros-plugin",
    "mkdocs-include-markdown-plugin",
    "pymdown-extensions",
]

[project.urls]
homepage = "https://github.com/auto-py-utils/can-log-analyzer"
repository = "https://github.com/auto-py-utils/can-log-analyzer/"
documentation = "https://auto-py-utils.github.io/can-log-analyzer"

[project.scripts]
can-log-analyzer = "can_log_analyzer.app:main"

[tool.uv]
package = true

[tool.ruff]
line-length = 120

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
